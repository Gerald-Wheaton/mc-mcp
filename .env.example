# HTTP server port (default: 3000)
PORT=3000

# MC API base URL (default: https://api.maintenanceconnection.com/v8)
# Override only if connecting to a non-production MC environment
# MC_BASE_URL=https://api.maintenanceconnection.com/v8
