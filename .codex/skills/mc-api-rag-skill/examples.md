# Example prompts for the skill

## Endpoint discovery

- How do I upload a document to an asset?
- Which endpoint returns inventory by filter?
- What route should I use to create a purchase order line item?

## Exact route lookup

- What does `GET /Assets/{PK}` return?
- What parameters does `POST /WorkOrders` need?

## Schema lookup

- What fields are in `ApiResponse`?
- What schemas are referenced by `WorkOrder`?

## Comparison

- Compare the asset document endpoints.
- What is the difference between creating and updating a work order?
